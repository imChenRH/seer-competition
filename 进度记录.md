# Demo 制作进度记录

> 从 Demo 制作规划定稿后正式开始制作（2026-08-12）起的所有进度。每完成一个事项追加记录。问题与卡点见《待办事项.md》。

## 2026-08-11（规划日）

- ✅ Demo 制作规划定稿：与沈鹏程博士会议评审逐条对齐（影子模式呈现、技能序列统一为 9 技能完整版、执行来源单一推进者原则、简化叉取 B 方案、Harness VLA 架构同构论据）
- ✅ 会议纪要理解与落地（纪要文件 MinerU_markdown_智能纪要…）
- ✅ AgentOS 提示词三版：Aily（主用）、OpenClaw（备用）、豆包办公模式（备用）
- ✅ Harness VLA 论文启发落地：补充材料 01（实证段+文献）、03（P11 条目）、Demo 决策记录 12、研究笔记 1.4 节
- ✅ GitHub 推送（提交 2d9ed89、3f71c16）

## 2026-08-12（执行 D1/D2）

### B 线（飞书侧）——全部完成
- ✅ 5 张多维表格建成（技能库 13 条 / Fallback 10 条 / 任务运行表 / 审计日志 / 叉车状态）
- ✅ Aily 智能体配置（人设提示词 + 多维表格工具 + 原语分工原则）
- ✅ 剧本 1 跑通（正常流程，50s，节拍达成，审计 12 条）
- ✅ 剧本 2 跑通（FB-F01 自动恢复，第 2 次重试成功，审计 15 条）
- ✅ 剧本 3 跑通（FB-F02→FB-F07 人工介入 + 告警消息，成功率统计正确）

### A 线（服务器）——环境与验证
- ✅ AutoDL 租机（内蒙古 RTX 4090 24G，数据盘 200G）
- ✅ 判定 docker 不可行（无 mount/unshare 权限 + docker hub 被墙）→ 转 pip 路线
- ✅ 系统盘满事故修复（conda envs 迁数据盘）
- ✅ pypi 卡死 → 清华镜像；torch cu130 驱动不兼容 → cu128
- ✅ 桥接脚本端到端验证通过（飞书多表 ↔ 桥接 ↔ 任务执行闭环，T-800 已完成；期间修复单选/文本字段格式、技能序列分隔符、进程自杀等 bug）
- ✅ 规控脚本状态机自测 PASS（9 技能序列、虚拟模式位姿积分、完成事件回调）

## 2026-08-13 凌晨（执行 D2 深夜）

- ✅ Fast-WAM 环境修复（torch 2.11.0+cu128，CUDA True）
- ✅ Fast-WAM 权重（12G）+ UMT5 文本编码器（10.8G 三分片）+ Wan2.2 VAE（2.7G）全部下载并本地组装（checkpoint 缺文件问题：tokenizer/UMT5/VAE 均从 hub 手动下载 + config 指向本地）
- ✅ **Fast-WAM 技术验证通过**：6B 模型 CPU 加载 + GPU 推理，输出 7 维动作 [1,7]，耗时 0.86s（未编译首次调用）——"Fast-WAM 不是 PPT"证据到手（期间解决：GPU 双份显存 OOM → CPU 加载+GPU 迁移；lerobot device 属性不同步 → 手动 sync）
- ✅ Isaac Sim 5.0.0 pip 安装完成但 headless 崩溃（carb make_fcontext，seccomp 拦截）
- ✅ Isaac Sim 5.1.0 pip 安装 + **headless 验证通过（13.6s 启动）**
- ✅ 官方 zip **6.0.1 下载（用户提供直链→nvidia.cn 国内 CDN，12GB）+ 解压 + post_install + headless 验证通过（24.4s）**——**正式采用 6.0.1**
- ✅ 场景搭建 v1 保存成功（60 prim，scene_v1.usd）
- ✅ 渲染问题定位与解决：5.1 缺 libGLU.so.1（apt 装 libglu1-mesa）；6.0.1 需驱动 ≥580.65.06（官网确认）——老机驱动 570 无法渲染
- ✅ **换新机（内蒙B区，驱动 580.105.08 + CUDA 13.0）**：用户把老机数据盘整体迁到新机（模型/envs/脚本零重装）；官方 multi_camera 示例出图验证渲染管线；**场景渲染成功**（3 张截图，像素分析确认画面内容正常）
- ✅ 协作消息表通道建成（与 AgentOS 多表直接对话，demo/agentos_chat.py send/read）
- ✅ 叉车场景 v2 保存（scene_v2.usd：差速底盘+门架+货叉+carrier，mast_lift 平移关节 + fork_tilt 旋转关节 USD 定义）
- ✅ 场景 v1 修正版（AgentOS 图片审核闭环：传送带移箱口外与箱门垂直、栈板重排、补光加强）
- ✅ 与 AgentOS 图片协作通道完全打通（附件字段 + agentos_chat.py --image 修复 + bitable_image 类型修正 + 5 分钟轮询任务）
- ✅ SimReady Warehouse-01 数据集开始下载（nvidia/PhysicalAI-SimReady-Warehouse-01，753 USD 资产 + 完整仓库场景）
- ✅ 叉车物理化 + 关节驱动测试跑通（ARTIC_ROOT_SET + DRIVE_SET + carrier 位置变化；参数需微调）
- ✅ WebRTC 直播验证结论：AutoDL 平台不可行（无 tun 设备 + 无 UDP 端口映射；6.0 端口为 TCP 49100 信令 + UDP 47998 媒体；TCP-only 云需企业认证或换云）——**不影响 Demo**：交付视频用服务器离屏渲染录制（截图渲染已全通）
- ✅ 多视角截图流验证通道（front/top/side/close + v3_top/v3_front 本地可看）
- ✅ 场景 v3（AgentOS 评审全采纳：叉车 2.6m、月台 12m、传送带 7.5m 外 8m 长、行驶标线、半开箱门、补光）
- ✅ **场景 v3 终审通过**（AgentOS 五项验收全过，可进入技能序列联调）
- ✅ 视频录制管线验证（叉车动作 60 帧离屏渲染 + ffmpeg 合成 forklift_demo.mp4）
- ✅ **Warehouse-01 下载完成**（14GB/8073 文件）：修复 snapshot_download repo_type="dataset" 参数缺失；学术加速（network_turbo）备忘已记录
- ✅ **场景 v4 重建完成**（SimReady 官方资产）：Pallet_A1 栈板×3 + Cardbox 纸箱 + DeluxeSafetyTape 警示带（PBR 材质）+ 叉车占位几何；**修复 SimReady 资产厘米单位问题**（reference 统一缩放 0.01，原 120 米巨栈板导致全黑）；渲染验证通过（shots_v4_final1/2.png）
- ✅ 三幕动画雏形录制完成（act1_unloading.mp4 12s 正常卸货 / act2_recovery.mp4 9s 恢复 / act3_intervention.mp4 7.5s 人工介入，均已下载本地）
- ✅ Web 架构面板完成（demo/architecture_panel.html：三层卡片+建议vs执行双通道+审计日志流+指标+剧本按钮）
- ✅ 规控 + PhysX 真实关节驱动闭环（v4 场景物理化 + mast 关节 position drive + 300 帧录制 phys_mast.mp4；carrier 目标序列 0→0.55→0.15→1.05 全部到位）
- ✅ 仿真任务执行器（execute_task.py：9 技能序列驱动 + SKILL_DONE 事件流）
- ✅ **桥接真实模式端到端全通**（T-801：Aily 多表 sim 任务 → 桥接 SIMULATE=0 → Isaac Sim 真实执行 → 逐技能回写 → 已完成）——"仿真在环真闭环"实现
- ⏳ 主报告 / 四幕正式录制整合（飞书素材 + 仿真视频 + 面板）

## 当前状态快照

- **B 线**：就绪（录制时按 初始化→剧本1→2→3→仪表盘 走）
- **A 线**：Isaac Sim 6.0.1 ✅ / Fast-WAM ✅ / 规控脚本（逻辑 ✅，待接仿真）/ 桥接脚本（模拟模式 ✅，真实模式待接）
- **待办**：场景完成 → 截图验证渲染 → 叉车模型（缺，用户向仙工要）→ 规控接仿真 → 桥接真实模式 → Web 面板 → 四幕录制 → 主报告（8/15 AI 生成+人工审核）

## 2026-08-13 严格复核与局部重构（优先于上述历史口径）

- ❌ 撤回“T-801 证明九技能单次真实执行”的结论：旧桥接在九技能循环内每次又启动完整九技能 runner，实际重复执行九遍。
- ❌ 撤回“规控 + PhysX 真实关节闭环已证明”的结论：旧 `execute_task.py` 直接写 Xform，未调用非虚拟控制器；`phys_mast.mp4` 实际只有约 0.05 秒/一帧。
- ⚠️ 旧三幕录像和旧网页仅保留为历史素材：网页的进度、成功率与 190 ms 由定时器/常量产生，不是运行证据。
- ✅ Fast-WAM 保留为独立验证：远程日志证明模型输出 `[1, 7]`，本机第一次单次推理约 0.86 s；它尚未控制叉车，190 ms 不得写成实测。
- ✅ 新增严格声明—证据矩阵、单一 JSONL 事实源、三场景共用状态机、单次 runner 桥接、Z-up 剖视场景和只读操作台。
- ✅ 核心契约、三场景、桥接、仿真时间线、HTTP/API 与网页协议已建立自动化测试。
- ✅ Isaac Sim 6.0.1 新场景冒烟实跑：51.5 s、103 帧、640×360，事件 20 条且终态 `COMPLETED`；画面能看到进箱、取货、退出和放置。
- ✅ 新操作台已用真实 JSONL/录像在浏览器验收，不再硬编码成功状态。
- ✅ 三套 720p/8 fps 正式证据已再次重录：normal 457 帧/20 事件/`COMPLETED`，recovery 545 帧/24 事件/`COMPLETED`，intervention 289 帧/18 事件/`HUMAN_REQUIRED`；货叉 4° 倾斜和叉车 Z 轴偏航已实际写入 USD 变换并从场景观测复验。
- ✅ intervention 二次抽帧审核发现停车点语义不够安全，已改为箱外 `x=-1.0` 并重录；事件和画面均复验通过。
- ✅ 飞书现场任务 `T-DEMO-20260813-001` 只启动一次 Isaac runner，任务终态“已完成”，最后序号 19，审计恰好 20 条且连续 0–19。
- ✅ `demo/evidence/MANIFEST.json` 已机器复验所有 JSONL、视频帧率/帧数/时长、文件大小和 SHA-256，并纳入飞书操作员见证回执与 Fast-WAM 独立验证材料的哈希；飞书回执不冒充平台签名证明。
- ✅ 严格事件验证已覆盖三场景完整事件签名、成功/预期失败/Fallback/停车观测谓词、attempt、观测帧、来源/run-id/终态一致性和未知字段；桥接加入同主机同证据目录排他锁、稳定 bridge ID、先审计后推进检查点、全新运行目录和完整产物哈希收据。重启只能回放已封存证据，绝不静默重启 runner。
- ✅ Mac 最终回归通过：80 项 Python 测试、14 项 JavaScript 协议断言和三场景干运行均通过；将 `ResourceWarning` 提升为错误后仍通过。
- ✅ 真实浏览器终验：normal=`COMPLETED/9·9/0/51s`，recovery=`COMPLETED/9·9/1/62s`，intervention=`HUMAN_REQUIRED/2·9/2/36s`；三段正式视频均 `readyState=4`，intervention 页面显示 18 条连续审计事件与两次 `FB-F02` 完成。
- ✅ 对 Git 将纳入的 108 个文件执行凭证精确值与高危格式双重扫描，未发现密钥；`demo/.env` 和《凭证汇总.md》均为 0600 且被 Git 忽略。
- ✅ 完整工作已推送至公开分支 `captainNemoCheng/feature/seer-hvla-demo`，并向原仓库 `master` 建立草稿 PR #1；本地凭证文件未提交。
- ✅ 新增 `demo/项目总结与交付说明.md`，统一汇总目标、架构、重构内容、正式证据、验证结果、企业展示流程、能力边界与下一阶段计划。

## 2026-08-13 双层架构视频与物理语义升级

- ✅ 将仓库扩大到 44×28 m、10 组货架和阶段化内部机位；使用 SimReady Warehouse-01 的 concrete、metal、cardboard、rubber 四类物理材质。
- ✅ 托盘重构为两个与货叉中心 `y=±0.32 m` 对齐的开放叉孔；叉取状态不再只看几何位置。
- ✅ 正式 USD 新增 PhysicsScene、CollisionAPI、RigidBodyAPI、MassAPI、ArticulationRootAPI 和 `PhysicsFixedJoint`；`payload_attached=true` 同时要求几何对齐与挂接关节启用。
- ✅ 控制模式明确收窄为“确定性运动目标 + 显式物理挂接”，不是标定力控、接触规划或生产动力学。
- ✅ 生成 2560×1080 双层架构同步视频：左侧为 1280×720 Isaac 仓库作业，右侧为同一仿真时钟派生的大脑意图、小脑技能、Fallback、安全门和审计摘要；不展示隐藏思维过程。
- ✅ 操作台升级为“正常执行 / 自动恢复 / 安全介入 / Fast-WAM 验证”四页签，并增加 AgentOS 指令输入、技能分发、行为映射和审计游标。
- ✅ 对任意新输入执行 fail-closed：若指令与随附证据不一致，页面不会把它冒充为已执行任务，只切换回可审计指令。
- ✅ Fast-WAM 页只从本地脱敏日志读取 `[1,7]` 和 0.86 s，并同步展示不可外推边界。
- ✅ 新增 `demo/NEEDS-HUMAN.md`，列出企业模型、接口、安全链路、物理标定、叉车数据、部署网络和签署证据等必须人工补齐项。
- ✅ 本地自动化回归更新为 102 项 Python 测试与 20 项 JavaScript 协议断言；真实浏览器完成四页签、任务发送、输入防伪和视频播放验收。

## 2026-08-13 第二轮场景、偏航与对话界面修复

- ✅ 依据严格反馈继续采用“局部重构”：保留统一事件契约、九技能状态机和正式证据链，重做设施布局、叉车偏航、局部坐标观测与网页首屏。
- ✅ 集装箱、月台、传送带不再正对共线；集装箱约 `+8°`、传送带约 `-6°`，导航、载荷和相机误差统一在设施局部坐标计算，解决旧版 `yaw=0` 与路径未应用偏航的问题。
- ✅ 新增八类静态物理合同：地面、仓库外壳、货架、集装箱、月台、传送带、背景载荷和障碍物；正式场景各验证 186 个 Collision Prim，并保留“运动学演示、非动力学标定”的边界。
- ✅ 修正内部机位，关键进箱、叉取、出箱、放置画面不再被前景货架遮挡；按至少 `0.6 m` 车辆中心线净距进行布局和路径预检。
- ✅ 按飞书 AgentOS 参考图重构网页：首屏只显示四个模式、角色说明和输入框，发送后才展示任务分发、分屏视频和证据边界；页签切换会收起上一场景证据。
- ✅ 三份远端 Isaac Sim 6.0.1 正式证据与 2560×1080 分屏视频重新生成；normal 457 帧/57.125 秒，recovery 545 帧/68.125 秒，intervention 289 帧/36.125 秒。
- ✅ 新增 `demo/agentos对话模版.png`，保存本轮界面视觉依据；新增第二轮设计与两个实施计划，记录取舍、边界和验收标准。
- ✅ 第二轮最终回归：108 项 Python 测试、29 项 JavaScript 协议断言全部通过；三场景正式 JSONL、manifest 哈希、视频帧数/时长及设施偏航/碰撞摘要复验通过。
- ✅ 第二轮真实浏览器终验：首屏无历史证据；任意输入会恢复为可审计任务；normal/recovery/intervention 视频 `readyState=4` 且时长分别为 57.125/68.125/36.125 秒；Fast-WAM 仍严格限定为 `[1,7]`、0.86 秒独立验证。

## 2026-08-15 防穿模几何守卫升级

- ✅ 复现根因：运动学根节点直接写 Xform，PhysX 静态 CollisionAPI 不会阻止单段位置/偏航插值穿入传送带；旧路径在传送带侧面产生扫掠重叠，旧栈板目标还低于滚筒顶面约 0.23 m。
- ✅ 新增无第三方依赖的 2.5D OBB/SAT 碰撞核；相邻帧按平移 `≤0.025 m`、偏航 `≤0.5°` 细分，覆盖车身、双货叉、活动载荷、集装箱、月台、传送带、货架、背景载荷和故障障碍物。
- ✅ 传送带改为侧梁、支腿和三列窄滚筒，双叉道开放；集装箱与传送带载荷根高度分别为 0.125 m、0.785 m，均比支撑顶面高 0.005 m，挂接前后不再发生垂直瞬移。
- ✅ 传送带进场改为绕行、外侧预对正、固定 `-6°` 直线靠近；intervention 的车身和货叉均停在可见障碍物前；后排货物增加纵向间距。
- ✅ 时间线生成、Isaac 每帧 Xform 写入和 Manifest 三处失败关闭；右侧安全面板新增 `GEOMETRY CERTIFIED` 与最小净距。
- ✅ normal/recovery/intervention 分别执行 130447/142239/69642 次几何检查，禁碰数均为 0，最小车身净距为 0.150441/0.150441/0.193264 m。
- ✅ 本轮通过 110 项非网络 Python 测试、29 项 JavaScript 协议断言、compileall 与 diff-check；10 项 HTTP 测试仅因当前沙箱禁止本地 `bind()` 无法执行，非产品断言失败。
- ⏳ 正式 Isaac 重渲染已重试，但当前会话对 AutoDL SSH 返回系统级 `Operation not permitted`；因此现有 `20260813` 视频明确标为升级前证据，未篡改或冒充防穿模结果。远端恢复后需完成三场景重渲染、分屏合成、抽帧审核、Manifest、浏览器终验与公开分支推送。
