# SEER–HVLA Demo 四项升级交付报告

日期：2026-08-13

依据：《给 Codex 的 SEER–HVLA Demo 升级提示词》与仓库补充规格。

## 总体结论

继续采用“局部重构”，不推倒已有事件契约、九技能状态机、飞书桥接与证据体系。四项升级已达到企业交流 Demo 的可运行门槛，但规控物理化按任务书允许的路径降级为：

> 确定性运动目标 + 显式、可审计的 `UsdPhysics.FixedJoint` 叉取/释放状态。

因此不能宣称完成了轮式底盘、门架和货叉的标定 PhysX 闭环，也不能外推为真实叉车性能。

## 逐项验收

| 任务 | 结果 | 交付证据 | 降级或边界 |
|---|---|---|---|
| 1. 仿真物理化 | 演示级完成 | PhysicsScene、CollisionAPI、RigidBodyAPI、MassAPI、ArticulationRootAPI、FixedJoint；四种 SimReady 物理材质；双叉孔托盘 | ArticulationRoot 位于运动学刚体上，不构成有效动力学闭环；载荷位姿仍按确定性目标写入 |
| 2. 任务与场景升级 | 完成 | 44×28 m 仓库、10 组货架、阶段化内部机位、剖视集装箱、月台/传送带与全流程录像 | 叉车为可替换占位几何；异常仍为确定性注入 |
| 3. 规控程序升级 | 按允许降级完成 | 三场景共用控制时间线；观测从 Stage 变换、visibility 与 FixedJoint 状态反推；原事件契约和终态不变 | 未完成轮子 drive、门架 drive、货叉 PID 与企业参数标定 |
| 4. 网页展示升级 | 完成 | 四页签、AgentOS 对话框、任务分发、指令/行为/审计同步、三段 2560×1080 分屏视频、Fast-WAM 独立证据页 | Fast-WAM 机械臂页只展示接入路径；模型、任务数据与后训练待人类提供 |

## 关键真实性措施

1. `events.jsonl` 继续是唯一事实源；网页和右侧决策摘要不生成独立成功数字。
2. 叉取状态同时要求：托盘相对几何满足阈值、`physics:jointEnabled=true`。仅几何重合会 fail-closed。
3. 三段分屏视频与原始视频具有相同 fps、帧数、时长；右侧每帧按 `frame_index / fps` 投影事件。
4. 右侧展示的是任务目标、结构化意图、技能/Fallback、安全门和审计事件，不展示模型隐藏思维过程。
5. 网页输入若与随附任务证据不一致，不会伪造新执行；页面会提示并恢复为可审计指令。
6. Fast-WAM 页只解析完成标记齐全的本地脱敏日志，并同时显示不可外推边界。

## 正式证据

| 场景 | 原始片 | 分屏片 | 事件 | 终态 |
|---|---|---|---:|---|
| normal | 1280×720，409 帧，8 fps | 2560×1080，409 帧，8 fps | 20 | `COMPLETED` |
| recovery | 1280×720，497 帧，8 fps | 2560×1080，497 帧，8 fps | 24 | `COMPLETED` |
| intervention | 1280×720，289 帧，8 fps | 2560×1080，289 帧，8 fps | 18 | `HUMAN_REQUIRED` |

所有正式文件、视频探测结果与 SHA-256 记录在 `demo/evidence/MANIFEST.json`。

## 如何展示

```bash
./scripts/run_demo.sh serve demo/evidence 8765
```

打开 `http://127.0.0.1:8765/`：

1. 在“正常执行”发送已预填任务，观察技能分发并播放分屏片。
2. 切换“自动恢复”，讲解 `FB-F01`。
3. 切换“安全介入”，强调 `HUMAN_REQUIRED` 而非“失败后完成”。
4. 切换“Fast-WAM 验证”，只证明 `[1,7]` 单批输出和 0.86 s 首次单次调用，不宣称叉车或机械臂闭环。

## 待人类/企业输入

详细清单见 [`NEEDS-HUMAN.md`](NEEDS-HUMAN.md)。最高优先级是：真实叉车模型与物理参数、仙工控制/状态/安全接口，以及 Fast-WAM 机械臂或叉车后训练数据。
