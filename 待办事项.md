# Demo 验收门槛与待办

> 2026-08-13 严格审计后重写。不以“脚本跑过”或“画面像是”为完成，只以下列可复验证据结案。

## P0：正式证据

### 2026-08-15 防穿模升级（历史 V2，已由下方 V3 取代）

- [x] 车身、双货叉和活动载荷执行 2.5D OBB/SAT 扫掠检查，三场景本地认证零禁碰。
- [x] 修正传送带实心底座、滚筒支撑高度、栈板根高度、背景载荷间距和人工介入停车点。
- [x] 时间线生成、Isaac 每帧应用和 Manifest 对碰撞认证失败关闭。
- [x] 在 Isaac Sim 6.0.1 重新渲染三场景、生成中文分屏片、抽帧视觉复核并更新 Manifest；正式运行目录为 `isaac-*-20260815-v2-r4`。

### 2026-08-16 视觉与物理 V3（历史，已由 V4 取代）

- [x] 四轮改为横向圆柱并严格落地，叉车根全程 `z=0`。
- [x] 集装箱扩大到 7.5×3.4×3.5 m；集装箱、传送带与地面黄线平行。
- [x] 传送带改为 11 根横向圆柱滚筒、侧梁、横撑和支腿，释放后验证载荷受支撑且停稳。
- [x] 24 mm 主体拟合机位保持完整叉车入镜，并限制在货架间、顶梁下方的无遮挡走廊。
- [x] 网页视频/技能链约为 74:26，发送任务后自动滚动到视频区，移除“飞/聊/任/审”快捷栏。
- [x] 分屏右侧用蓝/琥珀/绿/红主题和更大字号区分运行、恢复、完成、人工接管。
- [x] 修正 FixedJoint 局部锚点与动态载荷世界旋转观测；正式运行日志无关节错位和无效 ArticulationRoot 警告。
- [x] V3 三场景重新渲染、抽帧复核并更新 Manifest；正式目录为 `isaac-*-20260816-v3-r2`。

### 2026-08-16 轮胎—地板合同 V4（当前）

- [x] 集装箱地板顶面调整到世界 `z=0`，与仓库地面齐平；初始托盘和插叉高度同步下调。
- [x] `2.5D_OBB_SAT_SWEEP_V3` 将四个轮胎纳入逐帧扫掠，只允许轮胎下切面与齐平地板产生不超过 1 mm 的支撑面接触。
- [x] 新增红绿测试：地板支撑面必须齐平、四轮必须进入动态包络、伪造车体下沉 1 cm 必须被认证拒绝。
- [x] V4 三场景在 Isaac Sim 6.0.1 重新渲染、抽帧复核、重建分屏视频并更新 Manifest；正式目录为 `isaac-*-20260816-v4-r1`。

- [x] 在 Isaac Sim 6.0.1 以 1280×720 / 8 fps 分别运行 normal、recovery、intervention。
- [x] 三次运行均保留 `events.jsonl`、`summary.json`、`scene.usda`、`simulation.mp4`，不把原始帧提交 Git。
- [x] 验证事件序号连续、仿真时间单调、只有一个末尾终态；normal/recovery 为 `COMPLETED`，intervention 为 `HUMAN_REQUIRED`。
- [x] 用 ffprobe 核对分辨率、帧率、帧数和时长，并抽取起、中、末帧做视觉验收。
- [x] 生成 `demo/evidence/MANIFEST.json`，写入运行环境、版本、场景、控制器、文件大小与 SHA-256。
- [x] 顶层 manifest 同时哈希飞书操作员见证回执与 Fast-WAM 脱敏日志；不把自填回执表述为平台签名证明。

## P0：飞书现场链路

- [x] 停止旧的九次重复 bridge，部署新的单进程桥接。
- [x] 任务表增加数值字段 `最后事件序号`，审计表按稳定 UUIDv4 幂等键追加。
- [x] bridge ID 稳定配置；成功运行以四产物哈希收据封存，重启只回放、缺收据失败关闭且 retry 必须使用新任务 ID。
- [x] 新建 `T-DEMO-20260813-001`，确认只启动一次 runner，全批 JSONL 校验后再回写终态。
- [x] 脱敏回执保存于 `demo/evidence/FEISHU_LIVE_RECEIPT.json`，未保留 token/密码。

## P1：本地可复现与展示

- [x] Mac 上 `./scripts/run_demo.sh check` 全通过，且没有 ResourceWarning。
- [x] 干运行可一键生成三场景证据，操作台仅从事件推导技能、Fallback、终态和时长。
- [x] 浏览器视觉、视频播放、空状态、CSP、路径穿越和控制台无报错验收通过。
- [x] 仓库扫描确认无 App Secret、密码、HF/NGC token 或 `demo/.env`。
- [x] README、进度、待办、计划和页面口径一致，不再宣称旧的 ROS 2/真实关节/Fast-WAM 叉车控制。
- [x] 三场景完整事件签名、Isaac 技能/Fallback/停车观测谓词、未知字段和终态后置条件均 fail-closed。

## 2026-08-15 合并前收尾修正

- [x] 分屏右栏按 `observed_frame / fps` 投影，不再用事件逻辑时钟直接对齐视频。
- [x] Isaac 后端动作时长从时间线观测帧派生，未来重渲染的事件时钟与视频一致。
- [x] 桥接排他锁支持 Windows（`msvcrt`）与 POSIX（`flock`）。
- [x] 测试中的 POSIX 路径断言改为跨平台断言。
- [x] 新增 `.gitattributes`，证据文本与源码统一 LF，Manifest 哈希不受 Windows CRLF 影响。
- [x] 明确 `FEISHU_LIVE_RECEIPT.json` 只证明 2026-08-13 现场任务，不代表 2026-08-15 录像现场见证。

## 下一阶段企业对接输入（不是本轮 Demo 结案阻塞）

- [ ] 向仙工索取真实叉车 USD/URDF/CAD、几何/质量/关节限位和负载参数。
- [ ] 确认底盘、门架、货叉、急停、安全 PLC 、状态与告警的 API/SDK/ROS 2 接口和频率。
- [ ] 获得可用于后训练的脱敏传感器、状态、操作和失败数据，再评估 Fast-WAM 影子模式。

## 明确不在本轮冒充完成的范围

- 仙工 SRC-5000 实机控制、真实 ROS 2 闭环、生产级感知/安全、动力学标定。
- Fast-WAM 叉车控制、叉车数据后训练、本机 190 ms 复现。
- 不得用网页动画、固定数字或无法对应 JSONL 的旧录像代替证据。

## 环境备忘

- 密钥只保存在本地已忽略的《凭证汇总.md》或 `demo/.env`，两者均必须是 0600，不进 Git。
- 当前正式云端环境：AutoDL RTX 4090、Isaac Sim 6.0.1；运行路径和端口以本地凭证文件为准。
- WebRTC 在当前 AutoDL TCP-only 条件下不稳定；正式展示使用离屏录像 + 实时 JSONL 操作台。
