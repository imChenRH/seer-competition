# 待办事项与问题记录

> 卡住的问题记录在此，解决后标注日期移到进度记录。原则：**不擅自降级计划**，解决不了就记录跳过，继续推进其他事项。

## 阻塞中（需要用户/外部）

- [ ] **场景重建（用户 2026-08-13 判定当前几何场景失败）**：用户自行下载官方资产库（SimReady Warehouse-01 / Containers & Shipping / 或 NVIDIA 页面），下载完成后用官方资产重建场景（集装箱/栈板/货物/仓库环境），替换全部占位几何
- [ ] **叉车 USD 模型缺失**——SimReady 资产库大概率没有叉车；用户会向仙工智能要。期间用简化几何叉车占位（差速底盘+门架+货叉 5 关节），USD 到位后替换
- [ ] **暂停与 AgentOS 交流**（用户指令 2026-08-13）：已删除 5 分钟轮询任务，等场景重建后再恢复
- [ ] 桥接脚本真实模式（SIMULATE=0）需要飞书 Aily Webhook URL（aily 告警工作流还没建）——录制前建好即可，模拟模式下告警消息由 Aily 直接发

## 进行中（我持续推进）

- [ ] 场景搭建 v1：脚本异常退出（无 Traceback，"SimulationApp.close() was not called explicitly"）——待诊断（疑似 Export/光照段错误）
- [ ] 场景截图验证渲染（headless 离屏渲染 + 相机，验证画面质量与光照）
- [ ] 规控脚本接 Isaac Sim（虚拟模式 → 真实驱动：订阅 /fork/contact 等，发布 /forklift/cmd_vel）
- [ ] 桥接脚本真实模式联调（Aily → 多表 → 桥接 → Isaac Sim 执行 → 回写）
- [ ] Web 架构面板（单 HTML：三层卡片 + 建议vs执行双通道 + 审计日志流）
- [ ] 四幕视频录制（服务器离屏渲染 + ffmpeg 录制；飞书画面本地 OBS）
- [ ] 主报告（8/15 用 AI 生成 + 人工审核；信息卡字段已定默认值）

## 已解决（移入进度记录）

- （见 进度记录.md 时间线）

## 环境备忘（复现用）

- **AutoDL 学术加速**（github.com / githubusercontent.com / githubassets.com / huggingface.co 代理）：
  - 终端：`source /etc/network_turbo`（启用）| `unset http_proxy && unset https_proxy`（取消）
  - Notebook：subprocess 读 proxy 环境变量注入 os.environ
  - 注意：加速可能影响正常网络，用完关闭；稳定性不承诺
- **HF 数据集下载**：`snapshot_download(..., repo_type="dataset")` 必须显式指定——默认按 model 仓库请求会 404；hf-mirror 对 dataset 的 revision API 有 bug，大文件下载优先 `source /etc/network_turbo` 走官方端点
- AutoDL 实例：connect.nmb1.seetacloud.com:42506，root，内蒙古 4090
- 数据盘 /root/autodl-tmp：conda envs（fastwam py3.12 / isaacsim py3.11）、models（Fast-WAM 全套）、bridge/、controller/、isaacsim601/（6.0.1 zip 版）
- Fast-WAM 加载配方：config device=cpu + load_text_encoder=False + CPU 加载后 policy.to(cuda) + 手动 sync device 属性 + 随机 context（uncond checkpoint）
- Isaac Sim：pip 5.0 在 AutoDL 崩溃（make_fcontext），5.1+ 可用；zip 6.0.1 亦可用（正式采用）
