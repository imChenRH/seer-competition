# CONTEXT — 项目术语表（只收术语决议，不含实现细节）

- **比赛**：2026 AI先锋未来人才大赛·仙工智能赛道（飞书 AI 大赛）。截止 2026-08-16（本周日）。评分：创新性 30% + 业务价值 30% + AI 深度 20% + 完整度 20%。
- **沈博**：沈鹏程博士，仙工具身研发负责人（企业 coach）。2026-08-11 会议纪要：《MinerU_markdown_智能纪要：飞书AI大赛参赛方案交流研讨_2026年8月11日》。认可我们的分层 VLA 架构、影子模式、Fallback；指定场景=大型前置仓储月台智能叉车集装箱卸货；指定仿真=Isaac Sim+OpenUSD（纪要语音转写误为"ASICsim"）；Demo 无需实体机器人，重点演示分层边界/平台/频率/无逻辑漏洞。
- **架构两分法（会议口径）**：上层=慢推理任务规划（端云结合），底层=细粒度执行 VLA（可部署于 100 多 TOPS 控制器 SRC-5000）。→ 我们的 Fast-WAM 应定位为"细粒度执行 VLA"（阶段2 端侧 156TOPS），OpenClaw 为"慢推理规划"（端云）。算力演进：阶段3 端侧 1000-2000 TOPS 全端部署。
- **参赛指南 v2 / 方案评估报告 v2**：已作废（2026-08-11 决议），不再作为执行依据。
- **Demo 载体**：智能叉车集装箱卸货系统（差速 AGV 叉车，对齐仙工 SFL 系列），非命题原的人形机器人。载体差异在视频中解释为"跨构型验证 AgentOS 可复用"。
- **豆包办公模式**：飞书内豆包，对话响应式，可读写多维表格（实测确认）。**在 AgentOS 中已降级为备用**（仅可能录一段对话素材），主链路不依赖。
- **OpenClaw（AgentOS 主载体，2026-08-11 决议）**：飞书**妙搭**一键搭建、运行在**飞书云**、经**飞书开发者平台**管理。接入模型 **seed2.0**。可通过飞书对话，具备**后台自动运行**能力（定时/轮询/自动触发）。承担 AgentOS 全部职责：对话决策（任务分解、Fallback 判断）+ 自动化推进 + 告警。
- **AgentOS（本 Demo 语境）**：搭在飞书上的任务与技能调度系统 = OpenClaw（决策 + 自动化，主载体）+ 多维表格（数据底座）+ 桥接脚本（ROS 2 侧）。妙搭/飞书云/开发者平台作为"飞书 AI 能力"叙事。
- **桥接脚本**：运行在 AutoDL 上的 Python 脚本，经飞书开放平台 API 轮询/回写多表 + ROS 2 驱动 Isaac Sim。OpenClaw 不直接碰 ROS 2，避免跨网络 DDS。
- **技能库（叉车版）**：13 项 FORK-* 技能（FORK-NAV/PER/OP/INT/SYS）。正式材料的人形版 15 项技能（NAV-*/MAN-*）不用于 Demo。
- **Fallback（叉车版）**：10 项 FB-F01…FB-F10。
- **三层安全验证**：运动学可行性 → 碰撞检测 → 人机安全距离（勿与"审计三层"混淆）。
- **审计层级**：决策层 / 安全验证层 / 执行反馈层。
- **影子模式**：Fast-WAM 建议通道与规控执行通道并行，Demo 呈现"建议 vs 执行"，不夸大模型当前能力。
- **Fast-WAM**：LeRobot 集成的世界动作模型（Wan2.2-5B backbone，~6B，推理 ~190ms）。LIBERO 全任务成功率 97.6%（LeRobot README / arXiv:2603.16666）。已知独立复现缺陷（tokenizer_model_id 内嵌路径），D1 需技术验证。
